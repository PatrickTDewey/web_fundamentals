class SLNode {
    constructor(value) {
        this.value = value;
        this.next = null
    }
}

class SLList {
    constructor(value) {
        this.head = null;
    }
    addToFront(val){
        var currentHead = this.head;
        var newNode = new SLNode(val);

    }
    addToBack(value) {
        if (this.head) {
            
        }
    }
}